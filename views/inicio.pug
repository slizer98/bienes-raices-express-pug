extends ./layout/app 

block styles
    link(rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css")

block contenido 
    div
        h1(class="text-center text-4xl font-extrabold") Ubicación en el Mapa
        div(class="flex flex-col md:flex-row items-center py-19 gap-4")
            h2(class="text-sm uppercase text-gray-800 font-bold") Filtrar propiedades 

            div(class="w-full md:w-auto flex items-center gap-2")
                label(for="categorias" class="text-sm uppercase text-gray-500 font-bold") Categorías
                select(id="categorias" class="flex-1 w-full bg-white border border-gray-300 text-gray-600 py-2 rounded-md shadow px-3 text-sm focus:outline-none")
                    option(value="") -- Seleccione --
                    each categoria in categorias
                        option(value=categoria.id)= categoria.nombre

            div(class="w-full md:w-auto flex items-center gap-2")
                label(for="precios" class="text-sm uppercase text-gray-500 font-bold") Precios
                select(id="precios" class="flex-1 w-full bg-white border border-gray-300 py-2 rounded-md shadow px-3 text-sm focus:outline-none")
                    option(value="") -- Seleccione --
                    each precio in precios
                        option(value=precio.id)= precio.nombre

                    

        div(id="mapa-inicio" class="h-[500px] mt-5")

        section(class="py-5")
            h2(class="text-center text-4xl font-extrabold pt-10 ") Casas en Venta

            div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
                each propiedad in casas
                    include ./templates/card

        section(class="py-5")
            h2(class="text-center text-4xl font-extrabold pt-10 ") Departamentos en Venta

            div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
                each propiedad in departamentos
                    include ./templates/card

block scripts
    script(src="https://unpkg.com/leaflet@1.8.0/dist/leaflet.js")
    script(src="/js/mapaInicio.js")